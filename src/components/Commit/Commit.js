import React from "react";

const Commit = () => {
  return (
    <div>
      <h3>
        this is commit per challenge our move for country side problem then
        moves to like
      </h3>
    </div>
  );
};

export default Commit;
